// src/ui/bg/lightForm.ts  (placeholder — replaced in Task 4.x)
import type { BgFormDeps } from './types';
export function mountLightForm(host: HTMLElement, _deps: BgFormDeps): void { host.replaceChildren(); }
export function updateLightForm(): void { /* filled in Phase 4 */ }
