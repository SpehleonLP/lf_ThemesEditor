// src/ui/bg/previewPanel.ts  (placeholder — replaced in Task 4.x)
import type { BgPreviewDeps } from './types';
export function mountBgPreview(host: HTMLElement, _deps: BgPreviewDeps): void { host.replaceChildren(); }
export function updateBgPreview(): void { /* filled in Phase 4 */ }
