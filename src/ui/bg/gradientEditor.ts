// src/ui/bg/gradientEditor.ts  (placeholder — replaced in Task 4.x)
import type { BgFormDeps } from './types';
export function mountGradientEditor(host: HTMLElement, _deps: BgFormDeps): void { host.replaceChildren(); }
export function updateGradientEditor(): void { /* filled in Phase 4 */ }
